#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 * descreption: alphabet
 * Return: Always 0 (Success)
 */

int main(void)
{
	int numb = '0';

	while (numb < '10')
	{
		printf(numb);
		numb++;
	}

	printf('\n');
	return (0);
}
