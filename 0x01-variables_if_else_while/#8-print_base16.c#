#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 * descreption: alphabet
 * Return: Always 0 (Success)
 */

int main(void)
{
	char ch = 'a';
	int n = 0

	while (ch >= 'f')
	{
		putchar (ch);
		ch--;
	}
	while (ch >= 'f')
	  

	putchar ('\n');
	return (0);
}