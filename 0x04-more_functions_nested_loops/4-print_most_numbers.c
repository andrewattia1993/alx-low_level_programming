#include "main.h"
#include <stdio.h>

/**
 * _isupper - check the code.
 * @c : a character either upper or lower case
 * Return: Always 0.
 */
void print_most_numbers(void)
{
	for (i = 0, i <= 9, i++)
	{
		_putchar(i + '0');
	}
	_putchar('\n');

	return (0);
}
